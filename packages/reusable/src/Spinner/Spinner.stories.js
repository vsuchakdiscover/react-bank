import React from "react";
import Spinner from "./Spinner";

export default { title: "Spinner" };
export const defaultExample = () => <Spinner />;
export const uncentered = () => <Spinner center={false} />;
